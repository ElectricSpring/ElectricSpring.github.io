{
  "name": "first-apk",
  "version": "1.0.0",
  "private": true,
  "description": "First APK - gioco stile Pac-Man per mobile",
  "scripts": {
    "build:web": "mkdir -p dist && cp index.html dist/index.html",
    "cap:sync": "npm run build:web && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
